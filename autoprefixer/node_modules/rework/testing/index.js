
var rework = require('..');
var fs = require('fs');
var str = fs.readFileSync('testing/index.css', 'utf8');

var vendors = ['-ms-', '-moz-', '-webkit-'];

str = rework(str)
  .use(rework.prefixSelector(''))
  .toString();

console.log(str);

